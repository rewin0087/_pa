<div id="setup-paper">
    <h5>SET-UP THE PAPER</h5>
    <form>
        <!-- PAPER SIZES -->
        <div class="form-group">
            <label for="">PAPER SIZE:</label>
            <select id="paper-size" class="fancy">
            </select>
        </div>
        <!-- PAPER TYPES -->
        <div class="form-group">
            <label for="">PAPER TYPE:</label>
            <select id="paper-type" class="fancy">
            </select>
        </div>
        <!-- PAPER COLORS -->
        <div class="form-group">
            <label for="">PAPER COLOR:</label>
            <select id="paper-color" class="fancy">
            </select>
        </div>
        <div class="form-group">
            <label for="">QUANTITY &amp; PRICE SELECTION:</label>
            <button type="button" id="qty-price" class="button-modal" data-toggle="modal" data-target="#qtyModal">
                <span class="mL5" id="qty-price-value-holder"></span>
            </button>
        </div>
    </form>
</div>
